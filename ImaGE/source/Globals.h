#pragma once

//#define A1_STUFF
//#define CONSTRUCT_BVH_EVERY_FRAME

template <typename T>
inline constexpr T EPSILON{ static_cast<T>(1e-5) };
template <typename T>
inline constexpr T WINDOW_WIDTH{ static_cast<T>(1600) };
template <typename T>
inline constexpr T WINDOW_HEIGHT{ static_cast<T>(900) };
